# 当前 Tk 桌宠 UI 优化清单

更新时间：2026-07-06

## 结论

当前版本 UI 先按“保留 Python/Tk、优化控件体系和信息架构”的路线推进，不再同步 WPF 产品版。截图中红框提到的 `CustomTkinter`、`ttkbootstrap` 只作为中期 Tk 现代化验证；`Avalonia`、`Qt for Python/PySide6`、`pywebview` 只作为未来重新选型时的备选，不覆盖当前 Tk 主线。

当前仓库分支为 `tk/main` 时，所有 UI 优化默认属于 Tk/Python 线。Vue/Tauri 与 Tk 是平级版本，需要在 `vue/main` 或 `feature/vue/*` 中单独实现；两条线各自验证后再合入 `main`，让主分支包含全部可公开提交内容。本轮执行计划见 `docs/product/蛋黄-Tk前端页面优化执行计划-v1.md`。

所有当前 Tk 版本新增 UI 功能和优化都视为产品资产，后续必须固化到 Tk 文档、运行镜像和 exe 包：

- 每次 UI 改动前，先在本文登记优化项、页面范围和验收口径。
- 每次 UI 改动后，补充截图或验收记录到 `qa/`。
- 每次 UI 改动必须检查按钮文字、状态 chip、长标题和长说明在常规窗口宽度下不重叠、不竖排、不横向溢出；不得依赖窄列硬塞操作按钮。
- 每次 UI 改动后必须同步 `data-dev/current-runtime/danhuang/`，并重新生成 Windows exe 包到 `packages/`；新包验证通过后只保留最新一组 Tk Windows 包目录和 zip，旧包直接清理，不在 `packages/` 累积。
- C 盘历史运行实例已同步到 `data-dev/current-runtime/danhuang/`；后续新增功能、UI 优化和试运行优先只在 E 盘完成。

## 参考输入

- 用户截图中红框一：短期/中期可参考 `CustomTkinter` 或 `ttkbootstrap`。
- 用户截图中红框二：跨平台或 Python 重 UI 可参考 `Avalonia`、`Qt for Python/PySide6`、`pywebview`。
- 当前审计结论：`run-danhuang-desktop-pet.py` 约 15945 行，`open_control_panel` 约 3874 行，控制面板已成为主要维护风险。
- 当前代码现状：已有暖色主题、自绘 `select_box()`、自绘 `panel_button()`、滚动正文、左侧导航、右键快捷面板和多宠物缩略图。
- 当前 E 盘运行镜像：`data-dev/current-runtime/danhuang/`；代码改动需要同步 `src-prototype/legacy-monolith/run-danhuang-desktop-pet.py` 和镜像主程序。

## 技术路线判断

| 路线 | 当前判断 | 使用方式 | 验证重点 |
| --- | --- | --- | --- |
| 现有 Tk + 自绘控件 | 近期主线 | 先统一控件、导航、页面密度和弹窗，不改透明桌宠窗口链路 | 语法、JSON、右键、下拉、滚动、错误日志、截图 |
| CustomTkinter | 中期候选 | 在独立 spike 验证控制面板壳、表单、按钮和切换控件 | 透明窗口、置顶、DPI、多屏、右键层级、打包体积 |
| ttkbootstrap | 中期候选 | 只验证是否能快速改善 ttk 视觉，不作为默认风格 | 是否偏 Bootstrap、是否破坏暖色陪伴感、下拉稳定性 |
| Avalonia | 远期重新选型备选 | 不先投入正式实现 | Windows 原生贴合度、透明窗口、多屏 DPI、打包 |
| PySide6 / Qt for Python | Python 重 UI 备选 | 适合复杂面板和图形能力验证 | 打包体积、样式体系、透明桌宠窗口、资源占用 |
| pywebview | 面板复杂到需要 Web UI 时备选 | 只考虑控制面板，不直接替代宠物透明窗口 | 窗口层级、焦点、内存、离线资源、打包 |

## 可优化点总览

| 优先级 | 模块 | 当前问题 | 优化方向 | Tk 产品化要求 |
| --- | --- | --- | --- | --- |
| P0 | 控件体系 | `panel_button()`、多个 `modal_button()`、普通 `tk.Button`、`todo_select_box()` 并存 | 抽统一 UI token 和组件工厂：按钮、输入框、下拉、滑杆、开关、卡片、状态条 | 先落 Tk 组件工厂，改完同步运行镜像并打 exe |
| P0 | 信息架构 | 左侧导航项目多，日常/高级混在一起 | 导航分组为日常、宠物资产、智能与提醒、设置与安全；高级项折叠 | Tk Shell 默认首页和分组导航必须截图验收 |
| P0 | 首页 | 当前“陪伴”承担首页职责，但名称和内容边界不清 | 新增或明确“首页/总览”：当前宠物、今日陪伴、AI、提醒、快捷动作 | Tk 首屏必须有同等总览，不直接进入设置页 |
| P0 | 弹窗与反馈 | 多处仍使用 `messagebox`，风格和交互割裂 | 做暖色本地 Modal/Toast：确认、错误、成功、异步进度 | Tk 内统一 Dialog/Toast，不使用散落系统弹窗 |
| P0 | 截图验收 | UI 改动缺少稳定截图基线 | 建立每页截图验收：首页、形象、档案、故事、行为、对话、AI、提醒、动作、操作、安全；重点检查按钮文字、状态 chip 和长说明不重叠、不竖排、不横向溢出 | 每次大 UI 改动至少留下相关页面截图和 exe 包 |
| P1 | 桌宠本体运动 | 默认低打扰、主屏边缘活动和多屏尺寸锁定已完成首轮；0.11.66 已补边缘巡游固定轴锁定和拖动方向清理，仍缺真实拖动/巡游视频验收 | 继续补桌面运动截图/录屏基线、拖动方向、边缘巡游、多屏拖动和惯性手感验证 | Tk 桌宠本体必须小尺寸、低打扰、边缘不漂移、拖动不抽搐、多屏尺寸稳定 |
| P1 | 右键快捷面板 | 宠物切换和扩展动作收纳已完成首轮显式化，仍缺截图验收 | 固定显示当前宠物、常用入口、基础动作、扩展动作、更多入口；继续优化视觉密度和截图基线 | Tk 右键菜单保留同样分组、显式切换和更多动作弹层 |
| P1 | 提醒页 | 快速新增、筛选、列表卡片、选中态、编辑弹层、到点弹窗和时间轴窗口已完成首轮；0.11.57 已修复未选中操作静默失败和“1440 分钟后”反馈；0.11.58 已将删除确认改为暖色面板确认；0.11.67 已补到点弹窗网格、快捷键和焦点处理 | 继续完善列表/弹窗截图基线和提醒页键盘焦点 | Tk 保留显式当前项、编辑弹层、到点弹窗、稍后提醒和时间轴 |
| P1 | AI 页 | Provider 状态卡、模型/连接/Key/测试结果分段、测试中禁用态、替换/清除 Key 本地确认、清空记忆面板确认和测试 Toast 已完成首轮，仍缺完整截图验收 | 补 AI 页截图基线，继续抽资料查询/时间工具反馈组件 | Tk 保留 Provider 状态机、错误恢复文案、测试中禁用态、Key 隐私边界和陪聊记忆清空边界 |
| P1 | 形象页 | 主形象、现实照片和列表已完成首轮分区；0.11.61 已将删除主像素图和删除参考图改为暖色面板确认；0.11.72 已将更换主像素图失败反馈改为面板 Toast，仍缺新增宠物向导截图验收 | 继续完善新增宠物向导、拖拽落点和截图基线 | Tk 资产页沿用概览、当前主形象、固定操作列表、现实照片分区和照片删除边界说明 |
| P1 | 动作页 | 动作预览和右键动作配置已完成首轮分离；0.11.62 已将清空动作精灵图改为暖色面板确认，仍缺上传 QA 结果卡和截图验收 | 继续补上传/清空后的可见 QA 结果、动作包管理和截图基线 | Tk 需要动作预览、动作包管理、右键动作配置和扩展动作清空边界四者分离 |
| P1 | 故事/档案 | 故事页和档案页已完成首轮降密度；0.11.63 已将故事删除确认和故事页关键提示改为暖色面板组件，截图基线仍需补齐 | 建立故事/档案截图基线，继续抽详情阅读器、状态 chip 和二级窗口 Toast 组件 | Tk 档案页同步宠物级数据隔离、故事摘要、聊天详情、全文阅读器和故事删除边界 |
| P1 | 安全页 | 个人备份、公开分发边界、导出状态和结果路径已完成首轮显式化；0.11.64 已将个人备份/恢复/精灵图备份反馈改为面板 Toast，0.11.65 已将导出安装包弹窗结果和 GitHub 前置错误改为状态卡 + Toast，仍缺完整截图验收 | 继续补导出截图基线、清理剩余系统弹窗和导出进度截图 | Tk 安全页保留分发安全边界、个人备份、公开分发规则、导出进度和结果路径 |
| P1 | 外观页 | 外观总览、窗口行为、气泡颜色预设卡和保存 Toast 已完成首轮显式化，仍缺截图验收 | 继续补外观页截图基线和 Tk token 对照 | Tk 外观页保留总览、窗口行为、气泡预设卡、自定义颜色、即时预览和保存反馈 |
| P2 | 视觉细节 | 暖色已存在，但卡片边框、字号、按钮高度不完全一致 | 统一 4/8 间距、44px 点击目标、正文 9-10pt、标题 11-15pt、柔和阴影或边框层级 | Tk 设计 token 固化为颜色、字号、间距、圆角、边框层级 |
| P2 | 键盘与可访问性 | 当前更偏鼠标操作 | 补 Tab 顺序、Escape、Enter、焦点态、不可只靠颜色表达状态 | Tk 要补键盘导航、焦点态和高对比状态 |

## 分页面优化清单

### 1. 控制面板外壳

- 将窗口固定基线从单纯 `880x620` 升级为可配置尺寸策略：默认 920x640，小屏不溢出，最大化使用原生窗口状态。
- 左侧导航增加分组标题和高级折叠，低频项默认收纳：巡逻、外观、安全、生成/导出。
- 页头只保留页面标题、简短状态和置顶/最大化；全局保存、恢复默认、高级模式放到底部或设置页，避免每页都产生无关动作。
- 正文滚动保持现有机制，但页面切换后必须重置到顶部，滚动条只在需要时出现。
- 给页面加载失败的降级页保留，但补“查看日志位置”和“复制错误摘要”按钮。

### 2. 首页/陪伴

- 把“陪伴”拆成更清晰的首页：当前宠物卡、等级/经验、今日陪伴、AI 状态、提醒数量、快捷入口。
- 保留短句陪伴气质，不把首页做成后台仪表盘。
- 快捷入口最多 4-6 个：聊天、摸摸、提醒、AI、动作、切换形象。
- 已落地：0.11.59 “重置陪伴数据”改为暖色面板确认，文案明确不会删除聊天、待办或提醒；后续仍建议把危险入口继续下沉到档案或安全区。

### 3. 形象和宠物资产

- 已落地：当前宠物主预览放在页面顶部，展示主像素图、现实照片数量、动作包状态和主操作。
- 已落地：宠物列表拆成独立“家人形象列表”区域，行内使用状态、动作包、照片/动作数量 chip，描述在内容区换行。
- 已落地：取消形象列表窄操作列，操作按钮移入每张卡片内容区并使用固定像素宽度，避免“切换/编辑资料”被挤成竖排。
- 已落地：现实照片区只展示现实照片和补充参考，不展示 contact sheet、spritesheet 或生成中间图。
- 新增宠物导入继续保留 5 个基础动作要求，但优化为分步向导：资料 -> 主像素图 -> 参考照片 -> 基础动作 -> 校验结果。
- 已落地：新增宠物和编辑资料接入细分类模型，覆盖犬类、猫类、兔类、小型哺乳、大型哺乳、鸟类、爬行动物、水生生物、昆虫/节肢、人物、幻想生物、机器人、拟物/物件和其他。
- 已落地：基础动作槽位继续兼容旧 atlas，但 UI 和生图提示词按分类展示动作语义，例如人物是站立/走路/招手，鸟类是跳步/拍翅，水生生物是漂浮/游动，拟物角色是轻晃/弹跳。
- 已验收：`qa/tk-family-list-actions-inline-900-20260617.png`、`qa/tk-family-list-actions-inline-900-scrolled-20260617.png`，900px 宽度下形象列表按钮不竖排、不重叠。
- 已落地：0.11.61 删除主像素图和删除参考图从系统 `messagebox` 改为暖色面板确认；主像素图确认明确不会删除现实照片、参考图、动作精灵图或聊天记忆，参考图确认明确不会删除本机外部源文件。
- 已落地：0.11.69 当前主形象操作区和现实照片底部操作改为 `panel_button_grid()`，避免更换主像素图、添加现实照片、管理动作、编辑资料、删除主像素图等资产操作横向挤压。
- 已落地：0.11.70 添加现实照片窗口的空提交、复制失败反馈改为当前窗口 Toast，并补 `Esc` 关闭和焦点提升，减少私密照片入口的系统弹窗割裂感。
- 已落地：0.11.71 复制提示词、扩展动作提示词和使用教学的空内容、成功、失败反馈统一改为面板 Toast，减少动作/新增宠物辅助链路的系统弹窗割裂感。
- 图片上传区域保留拖拽和选择文件两种路径，错误提示必须落在当前区域内。

### 4. 对话、气泡和聊天

- 已落地：对话页首屏新增“对话总览”，区分快速试聊、AI 状态、记忆条数和当前气泡样式。
- 已落地：快速试聊区补空输入提示和发送状态，保留 Enter/发送按钮以及完整聊天窗入口。
- 已落地：AI 与记忆区使用状态卡表达云端可用、本地兜底、待配置或已关闭，并保留 AI 配置、测试、询问模型和查看记忆入口。
- 已落地：气泡样式改为可视预览卡，点击预览即可切换；颜色调整仍统一跳转到外观页。
- 已落地：聊天窗本体补齐暖色输入卡、内联空输入错误、发送中禁用和 AI 失败后的可恢复提示，和控制面板试聊区保持同一反馈模型。
- 已落地：聊天新增本地时间工具，问“现在几点/今天几号”时直接用本机时间回答；AI 快速回复补入当前时间和本地待办摘要。
- 已落地：联网查询触发词扩展到上网查、联网查、实时、最近、天气、股价、汇率等；AI 未连接时也会返回本地网页摘要。
- 已落地：通用知识问题会自动触发资料查询，包括“什么是、谁是、为什么、怎么、如何、多少、哪里、哪年、有哪些”等；搜索层同时使用 DuckDuckGo Lite 和中文维基百科 OpenSearch 兜底，陪伴类问题如“你是谁、想你、陪我”不会误触发查询。
- 已落地：资料查询补充中文维基摘要和 Bing 兜底；非实时知识类问题优先尝试实体百科摘要，实时新闻/价格/天气类问题继续以网页搜索为主，不强行走百科。
- 已落地：无 AI 本地资料回复展示来源类型和 URL，查询词清洗覆盖“什么是 / 谁是 / 是什么 / 是谁 / 什么意思”。
- 已落地：聊天窗快捷区新增“时间”“查资料”，让工具型交流有明确入口。
- 已落地：聊天窗新增能力状态条，显式展示云端、时间、资料、待办和本地兜底五类状态；云端按 Key/开关显示，资料按 AI 可用性显示“查后给 AI”或“本地摘要”，待办会显示未完成和今日数量。
- 已落地：聊天新增本地待办查询工具，问“今天有什么安排 / 待办有哪些 / 查看一下今天待办”时直接读取本地待办；查询意图优先于新增待办，避免误把“查看”写成新任务。
- 已落地：聊天窗快捷区新增“待办”入口，并压缩快捷按钮宽度，避免新增工具入口后横向溢出。
- 已落地：聊天窗快捷区改为 4 列自适应网格，两行稳定展示陪伴、时间、待办、资料和背景入口，后续增加天气、汇率等工具时不再依赖单行横向堆叠。
- 已落地：聊天窗“角色 Skill”扩展为 8 个泛化风格，包含蛋黄本色、技术导师、产品拆解、知识博主、短视频编导、研究助手、直说教练和运营写手；AI prompt 使用“宠物身份 + 当前角色风格 + 适用场景 + 角色边界”约束，不冒充具体真人达人。
- 已落地：角色 Skill 区改为 4 列两行按钮 + 当前角色说明卡，说明卡按窗口宽度动态换行；520px 最小宽度截图已验收，避免按钮、长说明挤压、竖排或重叠。
- 已落地：聊天窗顶层改为固定底部输入区的网格布局，历史区单独伸缩，快捷按钮和输入框不再被上方内容挤出窗口；全窗截图验收必须包含输入框和发送按钮。
- 已落地：0.11.68 聊天窗口重复打开时恢复窗口并重新聚焦输入框；聊天背景上传失败改为锚定背景窗口的暖色 Toast，背景窗口补 `Esc` 关闭和焦点提升。
- AI 未配置时展示本地陪伴兜底状态，不用错误感很强的弹窗。
- 待补：补对话页和聊天窗截图基线。

### 4.1 外观和窗口

- 已落地：外观页新增“外观总览”，把体型、透明度、窗口置顶状态和当前气泡配色放到首屏。
- 已落地：窗口设置改为“窗口行为”分区，继续保留体型、透明度、限制在屏幕内、窗口置顶和多屏尺寸锁定。
- 已落地：对话框颜色从小色块改为三列预设卡，卡片内直接展示气泡背景、描边和文字效果，并用“当前”文字标记选中态。
- 已落地：当前颜色预览区展示背景、描边、文字色值，预设或自定义颜色变化后立即刷新桌面气泡预览。
- 已落地：控制面板底部“保存”后显示暖色 Toast，明确外观、聊天、提醒和窗口设置已写入 E 盘运行配置。
- 待补：外观页截图验收基线、Tk 颜色 token 对照表。

### 5. AI Provider

- 已落地：选中厂商顶部显示状态卡，覆盖未配置、已保存 Key、测试中、可用和失败，并给出恢复建议。
- 已落地：Provider 选择、模型选择、连接配置、本机 Key 和测试结果分组，接口格式和环境变量名默认收起。
- 已落地：点击“测试并启用”后立即显示测试中状态，成功后沿用当前自动启用逻辑，失败时状态文案给检查 Key、模型、Base URL、网络或兼容模式的恢复动作。
- 已落地：替换已保存 Key 前增加确认，保存成功文案提示继续测试启用；真实 Key 继续不显示、不导出。
- 已落地：测试当前厂商时“测试并启用”按钮禁用并显示“测试中...”，异步结束后刷新 AI 页恢复可操作状态。
- 已落地：清除 Key 改为暖色本地确认弹窗，说明只清除本机加密 Key，不影响环境变量和聊天记忆；未保存 Key 时只给页内状态反馈。
- 已落地：替换 Key 改为暖色本地确认弹窗，说明新 Key 会覆盖旧 Key，旧 Key 不会显示；替换/清除共用同一套 AI 页确认弹窗样式。
- 已落地：0.11.60 清空陪聊记忆从系统 `messagebox` 改为暖色面板确认，文案明确只清空最近聊天、长期记忆摘要和学习表达，不删除 API Key、提醒、待办或陪伴等级。
- 已落地：测试成功/失败时显示控制面板暖色 Toast，和状态卡/测试结果文本共同表达连接结果。
- 已落地：资料查询能力说明补充本机时间直答、无 AI 时的本地网页摘要兜底、来源 URL 和多搜索源策略；Tk 继续作为“工具能力状态”保留。
- 待补：AI 页截图验收基线。

### 6. 提醒和待办

- 已落地：快速新增只保留标题、时间、重要程度和添加按钮；分类、重复、持续提醒和备注进入详情。
- 已落地：筛选区分为搜索行和分类/焦点/完成状态筛选行，不再把所有控件横向塞满。
- 已落地：待办列表从原生 `Listbox` 改为固定高度滚动卡片区，卡片显示标题、时间、分类、优先级、置顶、重复、稍后和持续提醒信息。
- 已落地：点击卡片选中并刷新详情，当前项用“当前”状态 chip 显式标记；筛选后当前项不可见时自动选中第一条可见待办。
- 已落地：详细/编辑弹层改为当前状态、事项和时间、提醒规则、备注、底部动作区五段结构，标题为空和时间格式错误使用弹层内状态提示。
- 已落地：“完成/重开、编辑、置顶、时间轴”与“稍后提醒、删除”分层，删除远离主动作。
- 已落地：0.11.57 补齐未选中待办时的统一 Toast，完成、编辑、置顶、稍后提醒和删除不再像坏按钮一样静默无响应。
- 已落地：0.11.57 稍后提醒反馈文案改为“15 分钟后 / 1 小时后 / 明天 / 2 天后”，不再把明天显示成“1440 分钟后”。
- 已落地：0.11.58 删除待办从系统 `messagebox` 改为暖色面板确认；提醒页和编辑弹层共用同一确认入口，删除仍保留时间轴记录。
- 已落地：到点提醒弹窗改为暖色提醒卡，显示状态 chip、分类、优先级、时间、提醒次数和备注预览；完成、打开提醒页、稍后提醒、关闭分层展示。
- 已落地：0.11.67 到点提醒弹窗状态 chip、主操作和稍后提醒改为固定网格；新增 `Ctrl+Enter` 完成、`1/2/3` 稍后、`Esc` 关闭，弹窗打开后聚焦“完成”按钮并在关闭时清理弹窗引用。
- 已落地：提醒时间轴窗口从纯文本改为摘要卡和倒序事件卡，显示事件类型、时间、标题、分类、优先级、到期时间、重复和稍后分钟数；无记录时展示空状态。

### 7. 动作和右键动作栏

- 已落地：动作预览区只表达当前动作能否播放、帧数、素材来源和右键栏状态，播放按钮继续使用浅色。
- 已落地：右键动作栏配置单独卡片表达“已加入右键”的状态，不在动作名称后堆状态词。
- 已落地：基础动作固定展示且不可删除；扩展动作支持更多、上传、清空、待补推荐。
- 上传动作条后显示校验结果：尺寸、帧数、透明边缘、方向语义、是否接入右键。
- 失败素材保留为资产记录，但默认不加入 `supported_actions`。
- 已落地：0.11.62 清空扩展动作精灵图从系统 `messagebox` 改为暖色面板确认；确认文案明确动作会从动作页、右键动作栏和当前形象可播放动作中移除，但不会删除基础动作、主像素图、现实照片、参考图或聊天记忆。

### 8. 右键快捷面板

- 顶部继续显示当前宠物头像和名称。
- 已落地：宠物切换从悬停弹层调整为点击“切换形象”按钮，减少误触和发现成本。
- 切换弹层内保留“管理形象”入口，用于进入完整形象页。
- 已落地：分组固定为：常用、基础动作、扩展动作、窗口。
- 已落地：扩展动作超过 4 个时显示“更多动作 +N”弹层，不再撑高右键面板。
- 更多动作弹层内可直接播放隐藏扩展动作，并保留“管理动作”入口。
- 点击内部按钮必须延迟执行目标命令，避免菜单销毁和页面打开冲突；现有外部点击检测继续保留。

### 8.1 桌宠本体运动

- 已落地：默认日常模式保持小尺寸、低频说话、低速巡游、主屏四边和不置顶，降低首次启动遮挡感。
- 已落地：0.11.66 边缘巡游目标记录当前边；每帧移动前锁住固定轴，减少沿屏幕边走时向屏幕中间漂移。
- 已落地：0.11.66 拖动松手进入惯性和惯性结束时重置拖动方向状态，减少左/右跑动方向残留。
- 已落地：`ui/tk_pet_window.py` 提供 `lock_edge_roam_position()` 纯函数，四边固定轴和普通夹取有单元测试覆盖。
- 待补：真实桌面拖动录屏、边缘巡游长时间录屏、多屏负坐标副屏拖动验证。

### 9. 安全、导出和安装包

- 已落地：安全页首屏先讲清“分发安全边界”，区分默认排除、弹窗可选和永不导出的数据。
- 已落地：个人备份和公开安装包分区，配置导出明确只用于本人迁移，不作为公开分发包。
- 已落地：安装包导出区提示默认干净包，目标电脑首次启动为空待办、空聊天、空 Key。
- 已落地：安全页新增“导出状态和路径”卡片，展示默认导出目录、弹窗结果路径说明和 `desktop-pet-error.log` 位置。
- 已落地：导出弹窗新增状态卡，覆盖就绪、导出中、成功、失败；成功后保留压缩包路径和入口脚本路径，失败时给日志路径和恢复建议。
- 已落地：Windows 本地导出和 macOS 源码包导出改为后台线程执行，导出期间禁用底部按钮，避免重复点击。
- 已落地：2026-06-17 基于 E 盘当前运行镜像准备重新生成 Windows 免 Python exe，公开包继续清空聊天、待办、提醒历史、陪伴状态和 API Key。
- 已落地：2026-06-17 已生成 Windows 免 Python exe 包，导出包只包含蛋黄默认资产和干净新用户模板。
- Token、DPAPI、聊天、待办、提醒历史和本机路径继续禁止进入公开包。
- 已落地：0.11.64 导出配置、恢复配置和备份精灵图统一使用面板 Toast；恢复失败不再弹 Windows 系统错误框，成功后显示友好路径标签。
- 已落地：0.11.65 导出安装包弹窗的导出完成、导出失败、缺少 GitHub 仓库、缺少 GitHub Token、macOS 远端构建完成/失败统一使用状态卡和面板 Toast，不再弹 Windows 系统信息框/错误框。
- 已落地：0.11.65 打包后 `packages/` 只保留最新一组 Tk Windows 包目录和 zip，旧包已清理。
- 待补：导出安装包进度截图、统一暖色确认弹窗和安全页截图验收基线。
- Tk 安全页必须保留导出清单、个人数据排除项和开机启动默认关闭策略。

### 10. 故事和档案

- 已落地：故事页保留宠物筛选和按日期折叠分组，默认展开最近日期。
- 已落地：故事、日记、思念列表项使用状态 chip 显示类型、时间和图片数量。
- 已落地：每条记录提供统一暖色“查看全文”阅读器，可查看完整正文和图片缩略图。
- 已落地：删除操作进入右侧危险区，和查看/编辑操作分离。
- 已落地：0.11.63 删除故事从系统 `messagebox` 改为暖色面板确认；确认文案明确只移除当前宠物的一条故事记录，不删除图片文件、聊天记忆、提醒、陪伴等级或其他宠物故事。
- 已落地：0.11.63 故事编辑器的空内容、图片/背景失败、无 AI、无故事和生成失败提示改为面板 Toast；二级编辑窗口通过 `show_panel_toast(parent=...)` 将反馈锚定到当前窗口。
- 已落地：档案页新增长期记忆摘要卡，显式展示宠物级数据隔离、摘要更新次数、主要情绪和记忆条目。
- 已落地：档案页最近聊天按日折叠为摘要行，显示时间、说话双方、情绪、记忆更新状态，并提供“查看详情”阅读器。
- 待补：建立故事/档案截图验收基线，后续把详情阅读器、状态 chip 和摘要卡抽为共享组件。

## 组件资产化要求

短期 Tk 版本需要先沉淀一套轻量 UI 组件，不继续在各页面复制 `tk.Button` 和颜色：

- `theme_tokens`：背景、卡片、文字、弱文字、主色、主色深、主色浅、鼠尾草、危险色、边框。
- `PanelButton`：primary、neutral、ghost、danger、selected、disabled。
- `PanelCard`：标题、说明、内容、危险区。
- `PanelSelect`：统一替代 `select_box()` 和 `todo_select_box()`。
- `PanelSlider`：统一替代 Tk 默认 `Scale` 和局部自绘滑杆。
- `PanelSwitch`：替代普通 Checkbutton，表达开关状态。
- `PanelDialog`：替代高频 `messagebox`，支持确认、错误、成功和异步进度。
- `StatusPill`：表达当前、已配置、待补、失败、重要、逾期等状态，不能只靠颜色。
- `ImageDropZone`：主像素图、参考照片、动作条上传统一落点。

后续 Tk 迭代时，这些组件应优先收敛为共享组件，而不是继续按页面硬写控件。

## 验收口径

每个 UI 优化批次至少完成：

- 文档：本文补充对应条目，必要时同步 `UI重构规格.md`、项目梳理和执行追踪。
- 静态验证：`python -m py_compile run-danhuang-desktop-pet.py`。
- 数据验证：settings、family、state、todos、providers JSON 可解析。
- 页面烟测：控制面板能打开目标页，切页滚动回顶部，文本不重叠，不横向溢出。
- 控件烟测：右键面板、下拉、滑杆、开关、上传入口、弹窗、危险确认可操作。
- 视觉验收：至少留下关键页截图到 `qa/`，大 UI 改动需要 contact sheet 或动作记录。
- 运行态：如果改 C 盘运行实例，验证后通过 `start-danhuang-desktop-pet.ps1` 重启当前桌宠。

## 建议实施顺序

1. 建立截图基线和页面清单，不先改样式。
2. 重排控制面板外壳和导航分组。
3. 抽 UI token 和组件工厂，统一按钮、输入、卡片、下拉、滑杆。
4. 优化首页、右键快捷面板、提醒页、AI 页四个高频入口。
5. 优化形象页、动作页、故事/档案页等资产管理入口。
6. 建立 CustomTkinter/ttkbootstrap 小型 spike，只验证控件壳，不替换主程序。
7. 每次完成 Tk 优化后，生成新的 Windows exe 包并记录 package 路径、zip 和 SHA256。

## 2026-07-03 执行计划落地

- 已新增 `蛋黄-Tk前端页面优化执行计划-v1.md`，明确本轮只优化 Tk/Python 线，Vue/Tauri 后续在独立分支处理。
- 已新增 `蛋黄-双版本分支与提交规范-v1.md`，固化 Tk/Vue 平级版本、文档归属、提交信息和 GitHub 推送边界。
- 后续 Tk UI 批次必须先补截图基线，再改共享控件和高频页面，避免再次出现按钮挤压、文本竖排和聊天输入框消失。
- 已完成第一批代码执行：对话页快捷试聊按钮和 AI 操作按钮改为统一 4 列网格，先消除这两处横向堆叠风险。

## 2026-07-05 第二批落地

- 当前执行根已切到 `E:\ProgrammingAlgorithm\VSCodeProjects\DanhuangProduct-tk`，并在该根下重建 `data-dev/current-runtime/danhuang/`；旧 `DanhuangProduct` 只作为本机资源来源，不作为本批运行或打包目标。
- Tk 版本更新到 `0.11.47`，继续复用 `panel_button_grid()`，首页/陪伴、行为、提醒、安全和底部操作条的高风险横向按钮组已改为固定列网格。
- AI 页 Provider 操作和测试结果操作改为网格布局，Key 为空、保存失败、测试前保存失败改为状态行 + 暖色 Toast 反馈。
- 已完成静态验证：legacy 与运行镜像 `py_compile`、27 个 modular 单元测试、`validate_phase3.py --pet-dir data-dev/current-runtime/danhuang --json` 通过，输出 5 个 ready 宠物、54 个可播放动作、11 个扩展动作且无 warnings。
- 已补真实 GUI 截图基线：`qa/tk-ui-0.11.47-20260705/` 覆盖首页、对话、AI、提醒、安全，Win32 外层窗口矩形截图下未见按钮竖排、重叠或横向截断。
- 已生成当前根 Windows 免 Python exe 包：`packages/danhuang-desktop-pet-windows-20260705-113909/` 和 `packages/danhuang-desktop-pet-windows-20260705-113909.zip`；SHA256 `18EE4396766D40335D6671D3596DB2C75690175EA66ADCD45FFA7228CD7B4012`。

## 2026-07-05 第三批付费 Beta 收口

- Tk 版本更新到 `0.11.48`，本批按付费用户严苛审计口径处理安装信任、首次体验、默认低打扰、主界面收口和隐私边界。
- 发行默认值改为低打扰：公开包默认不置顶、低频说话、低速巡逻、边缘活动、多屏巡逻关闭，且不再继承本机路径、GitHub 配置、跨屏设置和快捷动作。
- 右键菜单新增“安静一下”和“暂停活动/恢复日常”，用户能在被打扰时直接降噪，不需要先打开控制面板。
- 控制面板默认导航收口为首页、对话、提醒、形象、动作、安全；AI、档案、故事、行为、操作、设置、巡逻、外观进入高级模式或指定入口。
- 提醒页快速记录默认分类改为“生活”，时间格式错误和新增失败改为页内状态 + 暖色 Toast。
- 安全页新增“本地数据位置”卡片，提供打开数据文件夹、打开导出目录和个人备份入口。
- 公开包改为蛋黄单宠版：`pet-family.json` 只保留 `danhuang`，清空 `identity_image` 和 `reference_images`，不带用户上传源图、现实照片、身份参考图、跨宠物配置或扩展动作。
- 已补 0.11.48 干净包截图矩阵：`qa/tk-ui-0.11.48-20260705/` 覆盖首页、对话、提醒、安全、AI、形象、档案、故事、行为、动作、操作、设置、巡逻、外观、聊天窗口、右键菜单和提醒弹窗。
- 已生成当前根 Windows 免 Python exe 包：`packages/danhuang-desktop-pet-windows-20260705-122408/` 和 `packages/danhuang-desktop-pet-windows-20260705-122408.zip`；SHA256 `791D9F5409CF79CC45A0E2C594C473043FFB16EADC6F9E7886AAF0DE9B859EF8`。
- 历史旧包已在 0.11.64 批次按最新规则统一清理；当前 `packages/` 只保留最新 Tk Windows 包目录和 zip。

## 2026-07-05 第四批付费用户全流程严苛审计

- 严格问题：0.11.48 已经默认低打扰，但“安静 / 日常 / 活跃”仍只是行为页旧参数预设，且“安静”仍保留主动说话和巡游，付费用户会认为控制不可信。
- 严格问题：首页没有直接解释桌宠会不会打扰用户；用户必须进高级页才知道说话、巡游、置顶和屏幕中心策略。
- 严格问题：右键菜单只有临时“安静一下”和“暂停活动”，缺少持久的日常/活跃入口；桌宠正在桌面运动时，用户不能用最短路径调整陪伴强度。
- 已落地：Tk 版本更新到 `0.11.49`，新增共享陪伴模式模型，安静、日常、活跃三档统一控制自动说话、自动巡游、巡游间隔、速度、范围、置顶和屏幕中心策略。
- 已落地：首页新增“陪伴模式”卡片，展示当前模式和摘要，并提供三档切换；行为页旧开发者参数预设改为同一套模式入口。
- 已落地：右键菜单新增“陪伴模式”分组；“暂停活动/恢复日常”复用同一套模式逻辑，避免菜单、首页、行为页三处参数不一致。
- 已落地：右键菜单最小宽度从 `280` 提升到 `460`，修复新增模式分组后两列按钮裁切的真实截图问题。
- 已落地：公开包默认体型缩放从 `0.50` 收敛到 `0.46`，降低首次启动占屏和遮挡感。
- 已验收：生成 0.11.49 包 `packages/danhuang-desktop-pet-windows-20260705-131843/` 和 zip；截图矩阵补到 `qa/tk-ui-0.11.49-20260705/`，覆盖首页模式卡、行为页模式预设、右键模式分组、桌宠默认体型、聊天窗口和提醒弹窗。

## 2026-07-05 第五批设置可信度与旧配置迁移

- 严格问题：旧运行镜像没有 `activity_mode` 字段时，UI 会显示“日常”，但真实参数仍可能是旧的 90 秒说话、多屏巡游和允许进入屏幕中心，付费用户会认为模式按钮不可信。
- 严格问题：设置页仍以默认目录输入框和 GitHub macOS 构建表单为主，普通用户会看到工程路径、Token、workflow 等开发者信息，产品感弱。
- 严格问题：0.11.49 的桌宠默认体型和右键菜单截图没有处理 Windows 125% DPI，截图看起来像桌宠空白、右键右列被截断；这会误导验收结论，必须按物理像素重截。
- 已落地：Tk 版本更新到 `0.11.50`；缺失或无效 `activity_mode` 的旧设置会迁移到日常低打扰边界，当前运行镜像验证输出 `roam_interval=120`、`multi_monitor=false`、`center_allowed=false`。
- 已落地：设置页改为“本地文件夹”卡片，当前桌宠目录内路径显示为“当前桌宠目录”别名；GitHub macOS 构建改为开发者构建工具并默认折叠。
- 已验收：生成 0.11.50 包 `packages/danhuang-desktop-pet-windows-20260705-160513/` 和 zip；截图补到 `qa/tk-ui-0.11.50-20260705/12-settings-package-folded.png`，确认包内设置页不再直接铺开工程长路径和 GitHub 表单。
- 已验收：补充 0.11.50 DPI 修正截图 `qa/tk-ui-0.11.50-20260705/00-desktop-pet-dpi-scaled-window.png`、`16-right-menu-dpi-scaled-window.png` 和 `contact-sheet-0.11.50-dpi-qa.png`；桌宠本体可见，右键菜单两列完整。

## 2026-07-05 第六批右键高频入口压缩

- 严格问题：0.11.50 右键菜单虽完整，但在 125% DPI 下物理高度接近 941px，低矮屏幕和远程桌面窗口里底部暂停、隐藏气泡、退出入口容易被压到屏幕外。
- 严格问题：公开包只交付蛋黄单宠版，却仍展示“切换形象”入口，普通付费用户会认为软件有空功能。
- 已落地：Tk 版本更新到 `0.11.51`；右键菜单改为 3 列紧凑布局，布局 token 为 `columns=3/min_width=540/max_width=620/button_min_height=32`。
- 已落地：扩展动作主菜单只露出 3 个动作，其余进入“更多动作”，减少多宠/多动作配置下的右键菜单高度。
- 已落地：右键模型新增 `can_switch_pet` 契约，单宠包隐藏“切换形象”，多宠运行镜像仍保留切换入口。
- 已落地：包内使用说明按单宠/多宠生成不同文案，单宠版只提示形象页查看和管理资料。
- 已验收：生成 0.11.51 包 `packages/danhuang-desktop-pet-windows-20260705-162937/` 和 zip；截图补到 `qa/tk-ui-0.11.51-20260705/16-right-menu-compact-single-pet.png`，右键菜单逻辑尺寸 `540x593`，单宠 common commands 不包含 `open_pet_switcher`。

## 2026-07-05 第七批右键小屏滚动兜底

- 严格问题：0.11.51 的右键菜单高度已下降，但在 900x720 逻辑屏幕模拟下，如果只把整个按钮区做滚动，窗口操作仍可能落在初始视图下方；付费用户要先滚动才能暂停或退出，仍然不够合格。
- 已落地：Tk 版本更新到 `0.11.52`；右键布局新增 `max_height_ratio=0.78` 和 `min_scroll_body_height=180`，按钮区在小屏下自动压高并支持鼠标滚轮。
- 已落地：右键“窗口”分组前移到“常用”之后，确保“暂停活动 / 隐藏气泡 / 退出”先于陪伴模式、基础动作和扩展动作出现。
- 已落地：“AI 配置”右键入口改为“陪聊设置”，仍打开 AI 页，但避免把首屏做成接口调试台。
- 已落地：主程序 fallback 同步扩展动作上限为 3，并保留无 modular 目录时的高度计算，发行包离开开发目录后仍能保持同类右键布局边界。
- 已验收：生成 0.11.52 包 `packages/danhuang-desktop-pet-windows-20260705-164939/` 和 zip；截图补到 `qa/tk-ui-0.11.52-20260705/16-right-menu-small-screen-scroll.png`，900x720 逻辑屏幕模拟下右键菜单为 `540x561`、`scrollable=true`，窗口操作初始可见。

## 2026-07-05 第八批陪聊状态和安全页信任修复

- 严格问题：0.11.52 的 AI 页在 Key 缺失时仍显示“AI 对话 已开启”，这是付费用户最不能接受的状态自相矛盾。
- 严格问题：安全页首屏直接展示发行包所在 E 盘绝对路径，像开发者调试信息，不像可信的隐私说明。
- 已落地：Tk 版本更新到 `0.11.53`；AI 连接概览改为 Key 感知的“已关闭 / 待配置 / 已配置”，无 Key 时显示“云端陪聊 / 待配置”。
- 已落地：首页、对话页、操作页、导出弹窗和包内说明继续把普通用户入口收口为“陪聊设置 / 云端陪聊 / 陪聊服务配置”。
- 已落地：安全页本地数据位置改为“当前安装目录”和“当前安装目录 / exports”标签；完整路径仍可通过“打开数据文件夹 / 打开导出目录”进入。
- 已验收：生成 0.11.53 包 `packages/danhuang-desktop-pet-windows-20260705-170551/` 和 zip；截图补到 `qa/tk-ui-0.11.53-paid-trust-20260705/`，覆盖对话、AI 和安全页。

## 2026-07-05 第九批陪聊设置语言一致性修复

- 严格问题：0.11.53 入口已经叫“陪聊设置”，但进入页面后大标题仍是 `AI`，付费用户会明显感觉这是开发者配置页，不是消费级陪伴功能。
- 已落地：Tk 版本更新到 `0.11.54`；控制面板内部仍保留 `AI` 页面 key，展示层统一通过 `page_label()` 显示“陪聊设置”。
- 已落地：“陪聊设置 / 云端陪聊”加入页面别名，侧栏、页面标题、失败提示和 README 均不再把普通入口写成裸 `AI`。
- 已验收：生成 0.11.54 包 `packages/danhuang-desktop-pet-windows-20260705-172236/` 和 zip；截图补到 `qa/tk-ui-0.11.54-paid-language-20260705/ai-page-label.png`，确认最终发行包页面标题为“陪聊设置”。

## 2026-07-05 第十批陪聊文案和聊天输入区可见性修复

- 严格问题：0.11.54 的包内 FAQ、聊天发送提示和连接测试 Toast 仍有工程化口吻；普通用户会感觉自己在操作接口调试台，而不是陪伴产品。
- 严格问题：实测 560x680 聊天窗口时，角色 Skill 和说明区域过高，底部输入框和发送按钮可能被挤出可见截图；这是比文案更严重的核心聊天可用性问题。
- 已落地：Tk 版本更新到 `0.11.55`；包内说明、聊天发送提示、陪聊测试 Toast、失败状态和资料查询状态继续收口为“云端陪聊 / 陪聊服务”。
- 已落地：聊天窗口角色详细说明默认移出首屏，只保留当前角色摘要；聊天历史区初始高度降为 160，底部输入区保持可见。
- 已验收：生成 0.11.55 包 `packages/danhuang-desktop-pet-windows-20260705-173901/` 和 zip；截图补到 `qa/tk-ui-0.11.55-paid-copy-20260705/chat-composer-copy-final-scaled.png`，输入框、发送按钮和“当前云端陪聊/本地陪伴”提示完整可见。

## 2026-07-05 第十一批桌宠本体首次巡游信任修复

- 严格问题：0.11.55 默认“日常”模式写着低频巡游和 `roam_interval=120`，但主程序初始化仍硬编码 2-4 秒后开始自动巡游；付费用户刚启动就看到桌宠移动，会认为低打扰承诺不可信。
- 已落地：Tk 版本更新到 `0.11.56`；新增 `next_roam_timestamp()`，首次自动巡游与后续巡游统一走当前陪伴模式的间隔计算。
- 已落地：日常模式首次自动巡游延迟为 84-162 秒；右键“巡逻一下”仍保持立即执行，区分自动打扰和用户主动操作。
- 已验收：新增 0.11.56 回归测试，33 个 modular 单元测试通过；`qa/tk-ui-0.11.56-motion-20260705/initial-roam-schedule.json` 记录 legacy、当前运行镜像和发行包的首次巡游调度边界。
- 已验收：生成 0.11.56 包 `packages/danhuang-desktop-pet-windows-20260705-174959/` 和 zip；截图补到 `qa/tk-ui-0.11.56-motion-20260705/exe-smoke-desktop.png`，确认最终发行包 exe 可启动并显示桌宠本体。
