# 更新说明

## 0.11.64 - 2026-07-06

- 安全页个人备份反馈继续收口：导出配置、恢复配置和备份精灵图统一使用面板 Toast，避免个人数据迁移入口弹出割裂的 Windows 系统错误框。
- 恢复配置遇到无效 JSON、不可读文件或非蛋黄配置结构时，会显示可恢复的面板错误提示，不再直接抛出系统弹窗或中断流程。
- 导出配置和备份精灵图成功后会显示目标路径的友好标签，用户能确认备份已落盘。
- 本次只改个人备份/恢复/精灵图备份反馈，不改备份 JSON 数据契约、不扩大个人备份内容、不删除任何本机数据。

## 0.11.63 - 2026-07-05

- 故事页“删除故事”从 Windows 系统弹窗改为暖色面板确认，情感内容类危险操作体验与提醒、形象、动作和陪聊记忆保持一致。
- 确认文案明确只移除当前宠物的一条故事记录；不会删除图片文件、聊天记忆、提醒、陪伴等级或其他宠物故事，图片副本继续保留在宠物档案目录。
- 故事编辑器的背景设置失败、图片添加失败、空内容、无可用图片等反馈改为锚定当前编辑窗口的面板 Toast，避免系统弹窗打断写作。
- 故事摘要和角色提示词的无 AI、无故事、生成失败提示改为面板 Toast，保持故事页主界面的反馈一致性。
- `show_panel_toast()` 新增可选 `parent` 参数，供故事编辑器等二级窗口把 Toast 定位到当前交互窗口；旧调用保持兼容。
- 补充回归测试，确认 legacy 和当前运行镜像都不再使用 `messagebox.askyesno("删除故事"`、`messagebox.showwarning("内容为空"` 和角色提示词前置系统信息框。
- 本次只改故事页反馈、删除确认和回归测试，不改故事 JSON 数据契约、图片副本保存策略、AI Key/DPAPI、聊天记忆或提醒数据。

## 0.11.62 - 2026-07-05

- 动作页“清空动作精灵图”从 Windows 系统弹窗改为暖色面板确认，动作资产类危险操作体验与形象、提醒、陪伴和陪聊记忆保持一致。
- 确认文案明确该扩展动作会从动作页、右键动作栏和当前形象的可播放动作里移除；不会删除基础动作、主像素图、现实照片、参考图或聊天记忆。
- 未上传动作精灵图时改为面板 Toast 提示，不再弹系统信息框。
- 清空完成后显示面板 Toast，并继续重新加载动作帧、刷新右键快捷动作和保留重新上传入口。
- 补充回归测试，确认 legacy 和当前运行镜像都不再使用 `messagebox.askyesno("清空动作精灵图"`。
- 本次只改扩展动作清空确认、反馈文案和回归测试，不改动作 ID、动作 strip 格式、基础动作资源或宠物注册表结构。

## 0.11.61 - 2026-07-05

- 形象页“删除主像素图”和“删除参考图”从 Windows 系统弹窗改为暖色面板确认，宠物资产类危险操作体验与提醒、陪伴和陪聊记忆保持一致。
- 主像素图确认文案明确只处理桌宠目录里的主像素图副本；不会删除现实照片、参考图、动作精灵图或聊天记忆。
- 参考图确认文案明确不会删除本机外部源文件；只有桌宠目录里的上传副本会被清理，原始照片和故事记录不受影响。
- 未设置主像素图时改为面板 Toast 提示，不再弹系统信息框。
- 补充回归测试，确认 legacy 和当前运行镜像都不再使用 `messagebox.askyesno("删除主像素图"` 或 `messagebox.askyesno("删除参考图"`。
- 本次只改形象页资产删除确认、反馈文案和回归测试，不改宠物 ID、图片路径契约、参考图列表格式或动作资源结构。

## 0.11.60 - 2026-07-05

- “清空记忆”从 Windows 系统弹窗改为暖色面板确认，陪聊设置页的危险操作体验与提醒删除、陪伴数据重置保持一致。
- 确认文案明确只清空当前形象的陪聊记忆，包括最近聊天、长期记忆摘要和学习表达；不会删除 API Key、提醒、待办或陪伴等级。
- 清空完成后显示面板 Toast，并保留蛋黄气泡反馈；聊天记忆和记忆摘要 JSON 契约保持不变。
- 补充回归测试，确认 legacy 和当前运行镜像都不再使用 `messagebox.askyesno(f"清空{self.active_pet_name()}记忆"`。
- 本次只改陪聊记忆清空确认、反馈文案和回归测试，不改 Provider 配置、API Key/DPAPI 保存、提醒或陪伴状态路径。

## 0.11.59 - 2026-07-05

- “重置陪伴数据”从 Windows 系统弹窗改为暖色面板确认，首页/陪伴链路的危险操作体验与提醒删除确认保持一致。
- 重置确认文案明确只影响等级、经验、连续陪伴天数和互动次数，不删除聊天、待办或提醒。
- 重置完成后显示面板 Toast，并保留蛋黄气泡反馈；实际陪伴数据结构和重置后的初始字段保持不变。
- 补充回归测试，确认 legacy 和当前运行镜像都不再使用 `messagebox.askyesno("重置陪伴数据"`。
- 本次只改陪伴数据重置确认、反馈文案和回归测试，不改陪伴 JSON 数据结构、升级迁移或多宠物状态路径。

## 0.11.58 - 2026-07-05

- 提醒删除确认从 Windows 系统弹窗改为暖色面板确认弹层，提醒页和待办编辑弹层都使用同一套确认体验。
- 新增 `show_panel_confirm()` 通用确认 helper，后续高频危险操作可逐步复用，不再继续复制局部系统弹窗。
- 删除待办仍然是软删除：待办从列表隐藏，提醒时间轴保留删除记录；确认文案明确建议先考虑“稍后提醒”或“完成”。
- 面板 Toast 补充 `warning` 色板，未选中提醒、提醒不存在等轻警告不再落到普通信息样式。
- 本次只改提醒删除确认、面板反馈组件和回归测试，不改待办 JSON 数据结构、提醒历史契约或实际删除策略。

## 0.11.57 - 2026-07-05

- 提醒页待办操作不再静默失败；未选中待办时，完成、编辑、置顶、稍后提醒和删除会给出统一的面板提示。
- “稍后提醒”反馈文案改为普通用户可理解的时间表达，明天不再显示成“1440 分钟后”。
- 补充稍后提醒文案回归测试，确保开发镜像和当前运行镜像保持一致。
- 本次只改提醒交互反馈、宠物气泡文案和回归测试，不改待办 JSON 数据结构、提醒调度或历史记录契约。

## 0.11.56 - 2026-07-05

- 修复日常模式首次启动后 2-4 秒就可能自动巡游的问题；首次自动巡游现在遵守当前陪伴模式的巡游间隔。
- 日常模式默认首次自动巡游延后到约 84-162 秒区间，降低刚打开桌宠就被移动打扰的付费用户感受。
- 手动右键“巡逻一下”仍然立即执行，不影响用户主动让蛋黄活动。
- 本次只改巡游调度和回归测试，不改拖动、跨屏策略、宠物资产或本地数据契约。

## 0.11.55 - 2026-07-05

- 包内使用说明和聊天窗口高频状态继续收口为“云端陪聊 / 陪聊服务”，普通用户不再在 FAQ、发送提示和连接测试 Toast 里看到工程化故障提示。
- 陪聊失败、Key 未配置、资料查询后整理回复、角色 Skill 切换等状态仍保留恢复路径，但文案改为用户可理解的陪伴服务口径。
- 聊天窗口把角色详细说明从默认首屏移出，并降低聊天历史区初始高度，确保 520x660 到 560x680 窗口内底部输入区可见。
- 本次只改用户可见文案和版本号，不改 Provider 配置、API Key/DPAPI 保存、聊天记忆或本地资料查询数据契约。

## 0.11.54 - 2026-07-05

- 控制面板内部仍保留 `AI` 页面 key，但侧栏、页面标题和失败提示展示为“陪聊设置”，入口与落地页不再语言割裂。
- “陪聊设置”加入页面别名，刷新当前页或从展示名恢复页面时仍能回到原 AI Provider 配置页。
- README 当前功能说明同步为高级模式显示“陪聊设置”，避免用户手册回退到工程化命名。

## 0.11.53 - 2026-07-05

- AI/对话入口继续收口为“陪聊设置”和“云端陪聊”，对话页、首页、操作页和导出弹窗不再把普通用户入口写成“AI 配置”。
- AI 页连接状态改为 Key 感知：未配置 Key 时显示“待配置”，有 Key 时显示“已配置”，不再在 Key 缺失时显示“AI 对话已开启”。
- 安全页本地数据位置改为“当前安装目录 / exports”等友好标签，保留打开真实目录按钮，但不再把开发机完整绝对路径铺在首屏。
- 包内使用说明和导出说明同步为“云端陪聊/陪聊服务配置”口径，减少付费用户看到 API 调试台的感觉。

## 0.11.52 - 2026-07-05

- 右键菜单按钮区增加屏幕高度自适应：小屏、低矮远程桌面或内容过高时自动压到可用高度，按钮区可用鼠标滚轮滚动，避免底部“暂停活动 / 隐藏气泡 / 退出”跑出屏幕。
- 右键布局契约新增 `max_height_ratio=0.78` 和 `min_scroll_body_height=180`，并保留包内无 modular 目录时的主程序 fallback 计算。
- 右键入口“AI 配置”改为“陪聊设置”，降低普通用户第一次打开菜单时的工程感；目标页面仍为 AI 设置页。
- 主程序 fallback 扩展动作可见上限同步为 3，确保发行包和开发镜像的右键菜单密度一致。

## 0.11.51 - 2026-07-05

- 右键菜单改为 3 列紧凑布局，降低低矮屏幕和远程桌面窗口中底部“暂停活动 / 隐藏气泡 / 退出”不可见的风险。
- 扩展动作主菜单只展示前 3 个，其余进入“更多动作”，避免多扩展动作把右键菜单撑得过高。
- 单宠公开包不再显示“切换形象”空入口；只有导出包或运行镜像里存在多个可切换宠物时才展示切换入口。
- 包内使用说明会按单宠/多宠生成不同文案，单宠版只提示在形象页查看和管理资料。

## 0.11.50 - 2026-07-05

- 修复旧运行配置升级后“显示日常模式但实际仍按旧参数频繁说话、多屏巡游”的不可信问题；缺失或无效 `activity_mode` 时会迁移到低打扰日常边界。
- 设置页改为普通用户优先的“本地文件夹”卡片，默认目录显示为“当前桌宠目录”友好别名，不再把工程目录长路径直接铺在表单里。
- GitHub macOS 构建配置改为开发者构建工具，默认折叠；保存和写入 workflow 改为面板 Toast 反馈，减少系统弹窗割裂感。

## 0.11.49 - 2026-07-05

- 新增共享“陪伴模式”：安静、日常、活跃三档同时控制自动说话、自动巡游、置顶、边缘活动、移动速度和打扰间隔。
- 首页新增“陪伴模式”卡片；行为页旧开发者参数预设改为同一套模式入口，避免“安静”仍频繁说话。
- 右键菜单新增“陪伴模式”分组，可直接切换安静/日常/活跃；原“安静一下”继续作为临时静默入口。
- 右键菜单最小宽度从 280 提升到 460，避免新增陪伴模式后两列按钮被裁切。
- 安静模式会关闭自动说话和自动巡游；日常模式保留低频边缘活动；活跃模式提高互动频率但仍默认不置顶、不跑进屏幕中心。
- 公开包默认体型缩放从 0.50 收敛到 0.46，降低首次启动占屏和遮挡感。

## 0.11.48 - 2026-07-05

- 默认发行设置改为低打扰：不置顶、边缘活动、低频说话、低速巡逻，并清理公开包里的本机快捷动作和本机路径。
- 右键菜单增加“安静一下”和“暂停活动/恢复日常”，方便用户立刻降低打扰。
- 控制面板默认侧栏收口为日常入口，高级功能仍可通过高级模式或指定入口打开。
- 提醒页快速记录默认按生活事项处理，输入错误改为页内状态和暖色 Toast。
- 安全页展示本地数据目录和默认导出目录，安装说明明确免 Python 包优先、不写开机自启。
- 公开包清空宠物身份图和参考图字段，只保留蛋黄运行必需 spritesheet，避免用户上传源图或现实照片进入发行包。

## 0.11.47 - 2026-07-05

- 控制面板多处高频操作按钮改为固定列网格，包括首页/陪伴、行为、提醒、安全和底部操作条，降低小宽度下按钮挤压和横向溢出风险。
- AI 页连接、Provider 和状态操作改为网格布局，Key 输入缺失和保存失败改为页内状态 + 暖色 Toast 反馈。
- 当前根已重建 E 盘 Tk 运行镜像，后续验证和打包以 `DanhuangProduct-tk` 为准。

## 0.11.45 - 2026-07-03

- 宠物分类新增动作画像：身份重点、运动规则、禁用规则和质量检查，分类摘要和提示词会同步展示。
- 基础动作、单行动作修复、扩展动作提示词按分类输出动作细节，人物、鸟类、水生、机器人、物件、植物不再套用猫狗动作。
- 右键快捷动作和动作页基础动作栏会按当前宠物分类显示短标签，例如“招手”“拍翅”“摆鳍”“挥机械臂”“挪一小段”。
- 模块化核心补充分类动作画像函数，方便后续 Tk 拆分继续复用同一套规则。

## 0.11.44 - 2026-07-03

- 宠物分类升级为「一级大类 / 二级类型 / 三级细分 / 细节描述」，保留旧分类兼容。
- 新增 `category_group`、`category`、`category_subtype`、`category_detail` 字段，现有 5 个宠物已迁移到明确细分分类。
- 新增宠物和编辑资料弹窗改为分层分类选择，包含大类、类型、细分搜索和动作摘要，避免按钮挤压。
- 形象页当前宠物卡片新增体态、动作和基础动作摘要。
- 主图、基础动作、单行动作和扩展动作提示词会带入分类体态与动作语义，人物、机器人、水生、物件等不再套用猫狗动作。

## 0.11.32 - 2026-06-17

- 联网资料查询新增中文维基摘要补强，普通知识问题会优先尝试拿到更完整的百科摘要。
- 搜索来源扩展为 DuckDuckGo Lite、Bing 兜底、中文维基摘要和中文维基 OpenSearch，并合并去重。
- 无 AI 时的本地资料回复会显示来源类型和 URL，方便确认资料出处。
- 查询词清洗补充“什么是 / 谁是 / 是什么 / 是谁 / 什么意思”，让普通问句更容易转成可检索实体词。

## 0.11.31 - 2026-06-17

- 修复形象页家人形象列表右侧操作列过窄的问题，“切换/编辑资料”不再被挤成竖排文字。
- 每张形象卡片把操作按钮放到内容区底部，按钮使用稳定宽度；分类、照片和动作数量信息过长时会截断。
- 移除默认宠物代码里的历史本机图片路径，公开包继续不携带本机路径、API Key、聊天、待办或提醒历史。
- 补充 900px 宽度形象页截图验收，后续 UI 改动必须检查按钮、chip 和长说明不重叠、不竖排、不横向溢出。

## 0.11.30 - 2026-06-17

- 新增宠物和编辑资料支持细分类：犬类、猫类、兔类、小型哺乳、大型哺乳、鸟类、爬行动物、水生生物、昆虫/节肢、人物、幻想生物、机器人、拟物/物件和其他。
- 形象页会显示当前宠物分类；新宠物导入和资料编辑可以切换分类 chip。
- 五个基础动作槽位仍兼容旧 atlas，但提示词和界面会按分类解释动作语义，例如人物按站立/走路/招手理解，鸟类按跳步/拍翅理解。
- AI 宠物上下文和生图提示词加入分类、示例和动作方向，避免后续非猫狗形象被写成同一种动作逻辑。

## 0.11.29 - 2026-06-17

- 聊天窗新增“角色 Skill”选择区，可在蛋黄本色、技术导师、产品拆解、研究助手、直说教练、运营写手之间切换。
- 角色 Skill 会写入本机设置，后续 AI 回复自动按当前风格表达，同时保留蛋黄是家人的底层身份。
- 当前角色是能力风格 preset，不冒充具体真人达人，后续可继续扩展为本地 skill 或自定义角色。

## 0.11.28 - 2026-06-17

- 聊天新增通用知识问题识别：“什么是、谁是、为什么、怎么、如何、多少、哪里、哪年、有哪些”等提问会自动走资料查询。
- AI 可用时先查资料再交给模型回答；AI 未配置时返回本地网页摘要，减少普通问题答不上来的情况。
- 搜索层新增中文维基百科 OpenSearch 备用来源，并和 DuckDuckGo Lite 结果去重合并。
- “你是谁、你叫什么、想你、陪我、摸摸”等陪伴语境继续走宠物回复，不误触发网页查询。

## 0.11.27 - 2026-06-17

- 聊天窗快捷区改为 4 列网格，两行展示常用陪伴、时间、待办、查资料和背景入口。
- 工具入口不再依赖单行横向排列，窗口缩窄或后续增加快捷项时不容易溢出。

## 0.11.26 - 2026-06-17

- 聊天新增本地待办查询：问“今天有什么安排”“待办有哪些”“查看一下今天待办”时直接读取本机待办。
- 查询待办优先于新增待办，但“提醒我明天 9 点开会”这类创建意图仍会正常新增提醒。
- 待办查询回复显示标题、时间、分类、优先级和总览统计，AI 未配置时也能回答日程类问题。
- 聊天窗快捷区新增“待办”按钮，并缩短快捷按钮宽度避免横向溢出。

## 0.11.25 - 2026-06-17

- 聊天窗顶部新增能力状态条，展示云端、时间、资料、待办和本地兜底状态。
- 云端能力会按 AI 开关和 Key 配置显示已连接、待配置或已关闭；资料能力会提示当前是查后交给 AI 还是本地网页摘要。
- 待办状态显示未完成和今日数量，聊天创建本地待办后会刷新。
- 聊天窗关闭时清理状态条引用，避免重复打开后残留旧控件。

## 0.11.24 - 2026-06-17

- 聊天新增本地时间工具：问“现在几点/今天几号”时直接使用本机时间回复。
- 联网查询触发词扩展到上网查、联网查、实时、最近、天气、股价、汇率等；AI 未连接时返回网页摘要兜底。
- AI 快速回复补入当前时间和本地待办摘要，让日常安排、待办和资料类对话更顺。
- 聊天窗快捷区新增“时间”“查资料”；AI 页资料查询说明补充本地时间和无 AI 兜底。
- 控制面板保存后显示暖色 Toast，确认设置已写入 E 盘运行配置。
- Windows 免 Python 打包脚本补充应用图标导出，并在 PyInstaller 构建时使用 `danhuang-app-icon.ico`。

## 0.10.0 - 2026-05-24

- macOS 导出改为 Windows 触发 GitHub Actions `macos-latest` 构建 `.app`，完成后下载 `DanhuangDesktopPet-macOS-<version>.zip`。
- 导出弹窗支持配置默认导出目录、可选待办/故事/AI 非敏感配置，并保持 API Key 不导出。
- 新增“设置”页，集中管理默认导出目录、上传目录、图片目录和 GitHub macOS 构建配置。
- 故事编辑器支持拖入写作背景、图片素材插入正文标记，角色提示词查看窗口支持滚动。
- 新宠物和动作生图提示词强化主像素图基准、帧数、尺寸、连贯性、透明边缘和身份一致性要求。
- 动作上传增加追蝴蝶专项 QA：检测多余蝴蝶/碎片、脚底基线和体积漂移；异常追蝴蝶条运行时回退到稳定跑步帧。
- 新增提示词使用教学，说明主像素图、基础动作、单行动作修复和扩展动作的正确使用顺序。
- macOS 导出说明补充目标仓库 `owner/repo`、workflow、分支和 GitHub Token 权限配置。

## 0.9.0 - 2026-05-24

- AI 页改为顶部厂商下拉和当前厂商详情配置，修复厂商推荐文案串到其他 provider 的问题。
- 普通陪聊改为自然语言优先，不再要求 AI 返回 JSON；异常 JSON 回复会清洗后再展示。
- 明确“查一下、最新、资料、新闻、往年今日”等意图时才触发本地资料查询，再交给当前 AI 回复。
- 小米 MiMo 控制台改为平台控制台入口，API Base URL 明确标注不是网页控制台。

## 0.8.0 - 2026-05-23

- 新增“故事”页，支持按宠物记录故事、日记和思念，并生成故事摘要。
- AI 聊天注入当前宠物档案、故事摘要、角色提示词、最近故事和最近对话，避免跨宠物串记忆。
- 聊天窗改为微信式左右气泡，支持每个宠物独立聊天背景和透明度。
- 档案页按月份和日期归档聊天记录，减少长列表滚动。

## 0.7.0 - 2026-05-22

- AI Key 保存改为 DPAPI 本机加密，保存后只显示 `********`，不提供复制或显示真实 Key。
- 模型选择改为下拉，避免模型按钮横向铺开。
- 测试连接只验证当前 provider/model 能返回文本，不要求 JSON。
- 控制面板滚动区域统一重置，减少切页后出现大块空白。

## 0.6.0 - 2026-05-21

- 多宠物档案、等级、聊天和长期记忆改为按宠物隔离。
- 右键快捷菜单增加切换形象、控制面板、待办提醒和 AI 配置入口。
- 新增宠物导入支持主像素图、参考照片和五个基础动作条。
- 安装包导出增加使用说明，说明 AI 配置、隐私和常见问题。
